name: Update and deploy show calendar

on:
  push:
    branches: [main]
  schedule:
    - cron: "23 11 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: kingdom-circuit-pages
  cancel-in-progress: false

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run collector tests
        run: python -m unittest discover -s tests -p 'test_update_events.py' -v

      - name: Collect upcoming shows
        env:
          TICKETMASTER_API_KEY: ${{ secrets.TICKETMASTER_API_KEY }}
        run: python scripts/update_events.py

      - name: Save refreshed event data
        run: |
          git config user.name "kingdom-circuit-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add events.json run-status.json cache/ticketmaster-attractions.json
          if git diff --cached --quiet; then
            echo "No event-data changes to commit."
          else
            git commit -m "chore: refresh show listings [skip ci]"
            git push
          fi

      - name: Prepare single-page production site
        run: |
          rm -rf _site
          mkdir -p _site
          cp index.html styles.css app.js events.json run-status.json 404.html robots.txt sitemap.xml _site/
          cp -R assets _site/assets
          if [ -f CNAME ]; then cp CNAME _site/; fi
          test -s _site/index.html
          test -s _site/app.js
          test -s _site/events.json
          echo "Single-page Kingdom Circuit site prepared successfully."

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
