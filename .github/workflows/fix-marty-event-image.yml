name: Fix Marty event image

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: One-time repair completed
        run: echo "The Marty image repair is already committed."
