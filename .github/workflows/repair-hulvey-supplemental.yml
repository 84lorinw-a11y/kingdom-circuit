name: Repair Hulvey supplemental duplicates

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: One-time repair completed
        run: echo "The persistent Hulvey duplicate repair is already committed to the curation policy."
