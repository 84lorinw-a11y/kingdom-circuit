name: Fix Genesis image metadata

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: One-time repair completed
        run: echo "The Genesis image metadata repair is already committed."
