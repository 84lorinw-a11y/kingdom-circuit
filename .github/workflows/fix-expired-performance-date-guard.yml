name: Fix expired performance-date guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: One-time repair completed
        run: echo "The performance-date guard repair is already committed."
